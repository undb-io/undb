query GetForeignTableQuery($tableId: ID!) {
  table(id: $tableId) {
    id
    name
    schema {
      id
      name
      type
      defaultValue
      display
      constraint
      option
      metadata
    }
    views {
      id
      name
      type
    }
  }
}
